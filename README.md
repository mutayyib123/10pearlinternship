# Contact Management Frontend

Run locally:

```bash
cd frontend
npm install
npm run dev
```

This is a minimal Vite + React scaffold with pages: Login, Register, Dashboard, Profile.
Axios base URL is `http://localhost:8080/api` — adjust if your backend uses a different context path.
